
# Package

packageName = "SDF.nim.example"
version     = "0.1.0"
author      = "Cory Null(Noll) Crimmins - Golden"
description = "Sweep-and-update Euclidean distance transform functions - Example"
license     = "MIT"
bin         = @["example"]

# Dependencies

requires "nim >= 0.14.2"
requires "glfw >= 3.2.0"
requires "freeimage >= 0.1.0"
requires "opengl >= 1.1.0"
